<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, user-scalable=no" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="theme-color" content="#000000" />
    <meta name="apple-mobile-web-app-title" content="Timetable" />
    <link rel="manifest" href="manifest.json" />
    <link rel="apple-touch-icon" sizes="180x180" href="icons/icon-192.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="icons/icon-192.png" />
    <link rel="icon" type="image/png" sizes="512x512" href="icons/icon-512.png" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="format-detection" content="telephone=no" />
    <meta name="apple-touch-fullscreen" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Timetable" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-touch-startup-image" content="icons/icon-512.png" />
    <title>Semester 2 — Timetable</title>
    <style>
        :root {
            --red: #e63946;
            --blue: #457b9d;
            --dark-blue: #1d3557;
            --orange: #f4a261;
            --yellow: #e9c46a;
            --paper: #f5f5f0;
            --black: #000;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: var(--paper);
            font-family: "Futura", "Arial", sans-serif;
            padding: 40px 20px;
            color: #181818;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            font-size: 48px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 40px;
        }

        .page-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
            margin-bottom: 40px;
        }

        .page-header h1 {
            margin-bottom: 0;
        }

        .edit-toggle {
            background: transparent;
            border: 1px solid var(--black);
            color: var(--black);
            font-family: inherit;
            font-size: 9px;
            font-weight: 800;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            padding: 6px 12px;
            cursor: pointer;
            transition: background 0.2s ease, color 0.2s ease;
            -webkit-tap-highlight-color: transparent;
        }

        .edit-toggle[aria-pressed="true"] {
            background: var(--black);
            color: #fff;
        }

        .edit-time,
        .edit-activity {
            width: 100%;
            border: 1px solid #ddd;
            padding: 6px 8px;
            font-family: inherit;
            font-size: 14px;
            font-weight: 600;
            color: #222;
            background: #fff;
        }

        .edit-activity {
            min-height: 28px;
        }

        body.edit-mode .event {
            background: #fffdf5;
        }

        .day {
            margin-bottom: 30px;
            background: #fff;
            border: 3px solid var(--black);
            overflow: hidden;
        }

        .day-header {
            background: var(--black);
            color: #fff;
            padding: 15px 20px;
            font-size: 24px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
        }

        .day-header::before {
            content: "";
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }

        .monday .day-header::before { background: var(--red); }
        .tuesday .day-header::before { background: var(--blue); }
        .wednesday .day-header::before { background: var(--orange); }
        .thursday .day-header::before { background: var(--dark-blue); }
        .friday .day-header::before { background: var(--yellow); }

        .event {
            padding: 15px 20px;
            border-bottom: 2px solid var(--black);
            display: grid;
            grid-template-columns: 120px 1fr;
            gap: 20px;
            align-items: center;
            position: relative;
            background: #fff;
        }

        .event:last-child {
            border-bottom: none;
        }

        .event.empty {
            background: var(--paper);
        }

        .time {
            font-size: 16px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .activity {
            font-size: 16px;
            font-weight: 600;
            color: #222;
            min-height: 20px;
        }

        .event::before {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 8px;
            background: transparent;
        }

        .phy-255::before { background: var(--red); }
        .cos-210::before { background: var(--blue); }
        .cos-212::before { background: var(--dark-blue); }
        .wtw-211::before { background: var(--orange); }
        .wtw-218::before { background: var(--yellow); }

        /* Navigation */
        .site-nav {
            background: var(--black);
            padding: 0 20px;
            display: flex;
            align-items: center;
            gap: 0;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 3px solid var(--red);
        }

        .site-nav a {
            color: #999;
            text-decoration: none;
            font-size: 11px;
            font-weight: 800;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            padding: 16px 20px;
            transition: color 0.2s ease, background 0.2s ease;
            font-family: "Futura", "Arial", sans-serif;
        }

        .site-nav a:hover {
            color: #fff;
            background: rgba(255, 255, 255, 0.08);
        }

        .site-nav a.active {
            color: #fff;
            border-bottom: 3px solid var(--red);
            margin-bottom: -3px;
        }

        .nav-spacer {
            flex: 1;
        }

        .nav-label {
            color: #555;
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            padding-right: 10px;
            font-family: "Futura", "Arial", sans-serif;
        }

        @media (max-width: 768px) {
            body {
                padding: 0 0 80px 0;
                padding-bottom: calc(80px + env(safe-area-inset-bottom));
            }

            .container {
                max-width: 100%;
                padding: 15px 12px;
            }

            h1 {
                font-size: 28px;
                margin-bottom: 15px;
            }

            .page-header {
                margin-bottom: 15px;
            }

            .edit-toggle {
                font-size: 8px;
                padding: 6px 10px;
            }

            .site-nav {
                display: none;
            }

            .bottom-nav {
                display: flex;
                position: fixed;
                bottom: 0;
                left: 0;
                right: 0;
                background: var(--black);
                border-top: 3px solid var(--red);
                z-index: 200;
                padding-bottom: env(safe-area-inset-bottom);
            }

            .bottom-nav a {
                flex: 1;
                display: flex;
                flex-direction: column;
                align-items: center;
                gap: 4px;
                padding: 10px 5px;
                color: #666;
                text-decoration: none;
                font-family: "Futura", "Arial", sans-serif;
                font-size: 9px;
                font-weight: 800;
                letter-spacing: 1px;
                text-transform: uppercase;
                transition: color 0.2s ease;
                -webkit-tap-highlight-color: transparent;
            }

            .bottom-nav a.active {
                color: #fff;
            }

            .bottom-nav .nav-icon {
                font-size: 20px;
                line-height: 1;
            }

            .whats-next {
                display: block;
                background: var(--black);
                color: #fff;
                padding: 16px;
                margin-bottom: 15px;
                border-left: 5px solid var(--red);
            }

            .whats-next-label {
                font-size: 9px;
                font-weight: 800;
                letter-spacing: 2px;
                text-transform: uppercase;
                color: var(--red);
                margin-bottom: 6px;
            }

            .whats-next-class {
                font-size: 16px;
                font-weight: 700;
                margin-bottom: 3px;
            }

            .whats-next-time {
                font-size: 12px;
                color: #999;
                font-weight: 600;
            }

            .whats-next.done {
                border-left-color: #666;
            }

            .whats-next.done .whats-next-label {
                color: #666;
            }

            .day {
                margin-bottom: 12px;
                border-width: 2px;
            }

            .day-header {
                font-size: 16px;
                padding: 12px 15px;
                position: sticky;
                top: 0;
                z-index: 10;
            }

            .day-header::before {
                width: 28px;
                height: 28px;
            }

            .day.today {
                border-color: var(--red);
                border-width: 2px;
            }

            .day.today .day-header {
                background: var(--red);
            }

            .day.today .day-header::before {
                background: #fff !important;
            }

            .day.not-today {
                opacity: 0.55;
                transition: opacity 0.3s ease;
            }

            .day.not-today:active,
            .day.not-today:focus-within {
                opacity: 1;
            }

            .event {
                grid-template-columns: 90px 1fr;
                gap: 10px;
                padding: 12px 15px;
                border-bottom-width: 1px;
                min-height: 44px;
            }

            .event::before {
                width: 5px;
            }

            .time {
                font-size: 13px;
            }

            .activity {
                font-size: 13px;
            }

            .event.empty.hidden-empty {
                display: none;
            }

            .toggle-empty {
                display: inline-block;
                background: none;
                border: 2px solid var(--black);
                color: var(--black);
                font-family: "Futura", "Arial", sans-serif;
                font-size: 10px;
                font-weight: 800;
                letter-spacing: 1px;
                text-transform: uppercase;
                padding: 8px 14px;
                margin-bottom: 15px;
                cursor: pointer;
                -webkit-tap-highlight-color: transparent;
            }

            .toggle-empty.active {
                background: var(--black);
                color: #fff;
            }
        }

        @media (min-width: 769px) {
            .bottom-nav { display: none; }
            .whats-next { display: none; }
            .toggle-empty { display: none; }
        }

        .nav-label {
            display: none;
        }
    </style>
</head>
<body>
            <div id="orbitdesk-loader" role="dialog" aria-label="Loading OrbitDesk" style="position:fixed;z-index:9999;top:0;left:0;width:100vw;height:100vh;background:#fff;display:flex;align-items:center;justify-content:center;transition:opacity 0.6s;">
                <div style="display:grid;grid-template-columns:1fr 1fr;align-items:center;justify-items:center;width:100vw;height:100vh;">
                    <div style="display:flex;flex-direction:column;align-items:flex-end;justify-content:center;padding-right:2vw;">
                        <span style="font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-size:64px;font-weight:900;color:#e63946;line-height:1;letter-spacing:-2px;text-transform:uppercase;">Orbit</span>
                        <span style="font-family:'Helvetica Neue',Helvetica,Arial,sans-serif;font-size:64px;font-weight:900;color:#000;line-height:1;letter-spacing:-2px;text-transform:uppercase;">Desk</span>
                    </div>
                    <div style="display:flex;align-items:center;justify-content:center;">
                        <svg id="swiss-logo" width="160" height="160" viewBox="0 0 160 160" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <rect x="0" y="0" width="160" height="160" fill="#fff"/>
                            <rect id="swiss-bar" x="30" y="75" width="100" height="10" rx="5" fill="#e63946"/>
                            <rect id="swiss-bar-v" x="75" y="30" width="10" height="100" rx="5" fill="#e63946"/>
                            <circle id="swiss-dot" cx="80" cy="80" r="28" fill="#fff" stroke="#000" stroke-width="4"/>
                        </svg>
                    </div>
                </div>
            </div>
    <!-- Firebase SDK (compat for simplicity in a plain HTML file) -->
    <script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-firestore-compat.js"></script>
    <nav class="site-nav">
        <span class="nav-label">Navigate</span>
        <a href="dashboard.html">Dashboard</a>
        <a href="Semester1Timetable2026.html">Semester 1 Timetable</a>
        <a href="index.html" class="active">Semester 2 Timetable</a>
        <a href="planner.html">Day Planner</a>
        <a href="timer.html">Focus Timer</a>
    </nav>

    <div class="container">
                        <div style="display:flex;align-items:center;gap:18px;justify-content:center;margin-bottom:18px;">
                            <svg width="38" height="38" viewBox="0 0 38 38" fill="none" xmlns="http://www.w3.org/2000/svg" aria-label="OrbitDesk mini logo" role="img">
                                <rect x="0" y="0" width="38" height="38" rx="8" fill="#fff"/>
                                <rect x="8" y="17" width="22" height="4" rx="2" fill="#e63946"/>
                                <rect x="17" y="8" width="4" height="22" rx="2" fill="#e63946"/>
                                <circle cx="19" cy="19" r="8" fill="#fff" stroke="#000" stroke-width="2"/>
                            </svg>
                            <span style="font-family:'Futura','Arial',sans-serif;font-weight:900;font-size:1.3em;color:#1d3557;letter-spacing:2px;">OrbitDesk</span>
                        </div>
        <div class="bauhaus-construction">
            <div class="bauhaus-bg">
                <svg id="bauhaus-svg" width="100%" height="220" viewBox="0 0 600 220" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <defs>
                        <filter id="glow" x="-50%" y="-50%" width="200%" height="200%">
                            <feGaussianBlur stdDeviation="6" result="coloredBlur"/>
                            <feMerge>
                                <feMergeNode in="coloredBlur"/>
                                <feMergeNode in="SourceGraphic"/>
                            </feMerge>
                        </filter>
                    </defs>
                    <rect x="0" y="0" width="600" height="220" fill="#e63946"/>
                    <circle id="egg-circle" cx="120" cy="110" r="80" fill="#f4a261"/>
                    <rect id="egg-rect" x="320" y="40" width="180" height="140" fill="#457b9d"/>
                    <rect x="420" y="120" width="80" height="80" fill="#e9c46a"/>
                    <circle cx="500" cy="60" r="40" fill="#1d3557"/>
                    <!-- Impossible cursor animation shapes -->
                    <g id="impossible-cursor-group"></g>
                </svg>
            </div>
            <div class="bauhaus-text">
                <h1 id="egg-title">UNDER CONSTRUCTION</h1>
                <p>Semester 2 timetable is coming soon.<br>Check back later for updates!</p>
            </div>
        </div>

    <style>
        .bauhaus-construction {
            position: relative;
            min-height: 400px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin-top: 60px;
            margin-bottom: 60px;
        }
        .bauhaus-bg {
            width: 100%;
            max-width: 600px;
            margin-bottom: -40px;
            z-index: 1;
        }
        .bauhaus-text {
            position: relative;
            z-index: 2;
            background: rgba(245,245,240,0.95);
            padding: 32px 48px;
            border-radius: 24px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.08);
            text-align: center;
            animation: fadeInBauhaus 1.2s cubic-bezier(.68,-0.55,.27,1.55);
        }
        .bauhaus-text h1 {
            font-size: 56px;
            font-family: "Futura", "Arial", sans-serif;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 6px;
            color: #e63946;
            margin-bottom: 24px;
            animation: popTitle 1s cubic-bezier(.68,-0.55,.27,1.55);
        }
        .bauhaus-text p {
            font-size: 22px;
            font-family: "Futura", "Arial", sans-serif;
            font-weight: 700;
            color: #1d3557;
            margin-bottom: 0;
            animation: fadeInText 1.5s cubic-bezier(.68,-0.55,.27,1.55);
        }
        @media (max-width: 600px) {
            .bauhaus-bg svg {
                height: 120px !important;
                min-width: 0 !important;
            }
            .bauhaus-text {
                padding: 18px 8px;
                border-radius: 14px;
            }
            .bauhaus-text h1 {
                font-size: 28px;
                letter-spacing: 2px;
                margin-bottom: 12px;
            }
            .bauhaus-text p {
                font-size: 15px;
            }
            .bauhaus-construction {
                margin-top: 24px;
                margin-bottom: 24px;
                min-height: 180px;
            }
        }
        @keyframes fadeInBauhaus {
            0% { opacity: 0; transform: translateY(40px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        @keyframes popTitle {
            0% { transform: scale(0.7); opacity: 0; }
            80% { transform: scale(1.1); opacity: 1; }
            100% { transform: scale(1); }
        }
        @keyframes fadeInText {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }
        #egg-circle {
            transition: fill 0.5s, r 0.5s;
        }
        #egg-rect {
            transition: fill 0.5s, width 0.5s;
        }
        .egg-surprise {
            color: #f4a261 !important;
            animation: eggSurprise 1s infinite alternate;
        }
        @keyframes eggSurprise {
            0% { letter-spacing: 6px; }
            100% { letter-spacing: 16px; }
        }
    </style>
    <script>
                // Swiss Style OrbitDesk Loader Animation
                (function() {
                    const loader = document.getElementById('orbitdesk-loader');
                    const bar = document.getElementById('swiss-bar');
                    const barV = document.getElementById('swiss-bar-v');
                    const dot = document.getElementById('swiss-dot');
                    let t = 0;
                    let anim;
                    let minTime = 1200; // Minimum loader time in ms
                    let maxTime = 5000; // Maximum loader time in ms
                    let start = Date.now();
                    function animateSwissLogo() {
                        t += 0.06;
                        // Animate bars: grow/shrink and rotate
                        const h = 10 + 6 * Math.abs(Math.sin(t));
                        bar.setAttribute('height', h);
                        bar.setAttribute('y', 80 - h/2);
                        barV.setAttribute('width', h);
                        barV.setAttribute('x', 80 - h/2);
                        // Animate dot: pulse and slight move
                        const r = 28 + 4 * Math.sin(t*2);
                        dot.setAttribute('r', r);
                        dot.setAttribute('cx', 80 + Math.sin(t)*6);
                        dot.setAttribute('cy', 80 + Math.cos(t)*6);
                        anim = requestAnimationFrame(animateSwissLogo);
                    }
                    animateSwissLogo();
                    function hideLoader() {
                        loader.style.opacity = '0';
                        setTimeout(() => {
                            loader.style.display = 'none';
                            cancelAnimationFrame(anim);
                        }, 600);
                    }
                    // Hide loader after minTime if DOM is ready, else after maxTime
                    window.addEventListener('load', () => {
                        const elapsed = Date.now() - start;
                        setTimeout(hideLoader, Math.max(minTime - elapsed, 0));
                    });
                    setTimeout(hideLoader, maxTime);
                    if (window.history && window.history.pushState) {
                        window.addEventListener('popstate', () => {
                            loader.style.display = 'none';
                        });
                    }
                })();
        // Bauhaus SVG animation
        document.addEventListener('DOMContentLoaded', function() {
            const circle = document.getElementById('egg-circle');
            const rect = document.getElementById('egg-rect');
                (function() {
                    const loader = document.getElementById('orbitdesk-loader');
                    const bar = document.getElementById('swiss-bar');
                    const barV = document.getElementById('swiss-bar-v');
                    const barsGroup = document.getElementById('swiss-bars-group');
                    const dot = document.getElementById('swiss-dot');
                    let t = 0;
                    let anim;
                    const LOADER_KEY = 'orbitdesk_loader_shown';
                    const loaderAlreadyShown = sessionStorage.getItem(LOADER_KEY);
                    if (!loaderAlreadyShown) {
                        function animateSwissLogo() {
                            t += 0.06;
                            const h = 10 + 6 * Math.abs(Math.sin(t));
                            bar.setAttribute('height', h);
                            bar.setAttribute('y', 80 - h/2);
                            barV.setAttribute('width', h);
                            barV.setAttribute('x', 80 - h/2);
                            const rot = Math.sin(t/1.5) * 18;
                            barsGroup.setAttribute('transform', `rotate(${rot} 80 80)`);
                            const r = 28 + 4 * Math.abs(Math.sin(t*2));
                            dot.setAttribute('r', r);
                            dot.setAttribute('cx', 80);
                            dot.setAttribute('cy', 80 + Math.abs(Math.sin(t*2))*18);
                            anim = requestAnimationFrame(animateSwissLogo);
                        }
                        animateSwissLogo();
                        setTimeout(() => {
                            loader.style.opacity = '0';
                            setTimeout(() => {
                                loader.style.display = 'none';
                                cancelAnimationFrame(anim);
                            }, 600);
                        }, 3000);
                        sessionStorage.setItem(LOADER_KEY, '1');
                        if (window.history && window.history.pushState) {
                            window.addEventListener('popstate', () => {
                                loader.style.display = 'none';
                            });
                        }
                    } else {
                        loader.style.display = 'none';
                    }
                })();
        });
    </script>
            <!-- Impossible Cursor Animation for Bauhaus SVG -->
            <script>
            (function() {
                const group = document.getElementById('impossible-cursor-group');
                if (!group) return;
                // Only animate if SVG is visible
                const svg = document.getElementById('bauhaus-svg');
                if (!svg) return;
                // Reduce animation on mobile for performance
                const isMobile = window.innerWidth <= 600;
                const shapes = [
                    { type: 'circle', r: isMobile ? 8 : 16, color: '#fff', orbit: 60, speed: 0.012, phase: 0 },
                    { type: 'polygon', r: isMobile ? 10 : 20, sides: 3, color: '#e63946', orbit: 90, speed: 0.009, phase: 1 },
                    { type: 'polygon', r: isMobile ? 7 : 14, sides: 4, color: '#457b9d', orbit: 110, speed: 0.007, phase: 2 },
                    { type: 'line', len: isMobile ? 18 : 36, color: '#f4a261', orbit: 80, speed: 0.011, phase: 3 }
                ];
                let t = 0;
                function animate() {
                    t += 1;
                    group.innerHTML = '';
                    const cx = isMobile ? 180 : 300;
                    const cy = 110;
                    shapes.forEach((shape, i) => {
                        let angle = shape.phase + t * shape.speed;
                        let orbit = shape.orbit;
                        let x = cx + Math.cos(angle) * orbit;
                        let y = cy + Math.sin(angle) * orbit;
                        let el;
                        if (shape.type === 'circle') {
                            el = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
                            el.setAttribute('cx', x);
                            el.setAttribute('cy', y);
                            el.setAttribute('r', shape.r);
                            el.setAttribute('fill', shape.color);
                            el.setAttribute('filter', 'url(#glow)');
                        } else if (shape.type === 'polygon') {
                            el = document.createElementNS('http://www.w3.org/2000/svg', 'polygon');
                            let points = [];
                            for (let k = 0; k < shape.sides; k++) {
                                let theta = angle + (2 * Math.PI * k / shape.sides);
                                let px = x + Math.cos(theta) * shape.r;
                                let py = y + Math.sin(theta) * shape.r;
                                points.push(px + ',' + py);
                            }
                            el.setAttribute('points', points.join(' '));
                            el.setAttribute('fill', shape.color);
                            el.setAttribute('filter', 'url(#glow)');
                        } else if (shape.type === 'line') {
                            el = document.createElementNS('http://www.w3.org/2000/svg', 'line');
                            let x2 = x + Math.cos(angle + Math.PI/2) * shape.len;
                            let y2 = y + Math.sin(angle + Math.PI/2) * shape.len;
                            el.setAttribute('x1', x);
                            el.setAttribute('y1', y);
                            el.setAttribute('x2', x2);
                            el.setAttribute('y2', y2);
                            el.setAttribute('stroke', shape.color);
                            el.setAttribute('stroke-width', isMobile ? 3 : 6);
                            el.setAttribute('stroke-linecap', 'round');
                            el.setAttribute('filter', 'url(#glow)');
                        }
                        group.appendChild(el);
                    });
                    requestAnimationFrame(animate);
                }
                animate();
            })();
            </script>
    </div>

    <nav class="bottom-nav">
        <a href="dashboard.html">
            <span class="nav-icon">◻</span>
            Overview
        </a>
        <a href="Semester1Timetable2026.html">
            <span class="nav-icon">①</span>
            Sem 1
        </a>
        <a href="index.html" class="active">
            <span class="nav-icon">②</span>
            Sem 2
        </a>
        <a href="planner.html">
            <span class="nav-icon">▤</span>
            Planner
        </a>
        <a href="timer.html">
            <span class="nav-icon">◉</span>
            Focus
        </a>
    </nav>

    <script>
        const DAYS = ['sunday', 'monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday'];

        function highlightToday() {
            const today = DAYS[new Date().getDay()];
            const dayDivs = document.querySelectorAll('.day');
            let todayFound = false;

            dayDivs.forEach(day => {
                const dayClass = Array.from(day.classList).find(c => DAYS.includes(c));
                if (dayClass === today) {
                    day.classList.add('today');
                    todayFound = true;
                    if (window.innerWidth <= 768) {
                        setTimeout(() => day.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
                    }
                } else {
                    day.classList.add('not-today');
                }
            });

            if (!todayFound) {
                dayDivs.forEach(d => d.classList.remove('not-today'));
            }
        }

        function updateWhatsNext() {
            const banner = document.getElementById('whatsNext');
            const classEl = document.getElementById('nextClass');
            const timeEl = document.getElementById('nextTime');
            if (!banner) return;

            const now = new Date();
            const today = DAYS[now.getDay()];
            const currentMinutes = now.getHours() * 60 + now.getMinutes();

            const todayBlock = document.querySelector('.day.' + today);
            if (!todayBlock) {
                classEl.textContent = 'No classes today';
                timeEl.textContent = 'Enjoy your weekend';
                banner.classList.add('done');
                return;
            }

            const events = todayBlock.querySelectorAll('.event:not(.empty)');
            let nextEvent = null;
            let currentEvent = null;

            events.forEach(ev => {
                const timeText = ev.querySelector('.time').textContent;
                const [startStr, endStr] = timeText.split('-');
                const [sH, sM] = startStr.split(':').map(Number);
                const [eH, eM] = endStr.split(':').map(Number);
                const startMin = sH * 60 + sM;
                const endMin = eH * 60 + eM;

                if (currentMinutes >= startMin && currentMinutes < endMin) {
                    currentEvent = { el: ev, start: startStr, end: endStr };
                } else if (currentMinutes < startMin && !nextEvent) {
                    nextEvent = { el: ev, start: startStr, end: endStr };
                }
            });

            if (currentEvent) {
                const text = currentEvent.el.querySelector('.activity').textContent.trim();
                banner.querySelector('.whats-next-label').textContent = 'Right Now';
                classEl.textContent = text;
                timeEl.textContent = currentEvent.start + ' — ' + currentEvent.end;
                const cls = Array.from(currentEvent.el.classList).find(c => c.includes('-'));
                if (cls === 'phy-255') banner.style.borderLeftColor = '#e63946';
                else if (cls === 'cos-210') banner.style.borderLeftColor = '#457b9d';
                else if (cls === 'cos-212') banner.style.borderLeftColor = '#1d3557';
                else if (cls === 'wtw-211') banner.style.borderLeftColor = '#f4a261';
                else if (cls === 'wtw-218') banner.style.borderLeftColor = '#e9c46a';
            } else if (nextEvent) {
                const text = nextEvent.el.querySelector('.activity').textContent.trim();
                banner.querySelector('.whats-next-label').textContent = 'Next Up';
                classEl.textContent = text;
                timeEl.textContent = nextEvent.start + ' — ' + nextEvent.end;
                const cls = Array.from(nextEvent.el.classList).find(c => c.includes('-'));
                if (cls === 'phy-255') banner.style.borderLeftColor = '#e63946';
                else if (cls === 'cos-210') banner.style.borderLeftColor = '#457b9d';
                else if (cls === 'cos-212') banner.style.borderLeftColor = '#1d3557';
                else if (cls === 'wtw-211') banner.style.borderLeftColor = '#f4a261';
                else if (cls === 'wtw-218') banner.style.borderLeftColor = '#e9c46a';
            } else {
                classEl.textContent = 'Done for today';
                timeEl.textContent = 'No more classes';
                banner.classList.add('done');
            }
        }

        let emptyHidden = true;
        function toggleEmptySlots() {
            const empties = document.querySelectorAll('.event.empty');
            const btn = document.getElementById('toggleEmpty');
            emptyHidden = !emptyHidden;
            empties.forEach(e => e.classList.toggle('hidden-empty', emptyHidden));
            btn.textContent = emptyHidden ? 'Show Free Periods' : 'Hide Free Periods';
            btn.classList.toggle('active', emptyHidden);
        }

        const EDIT_STORAGE_KEY = 'timetable_sem2_edits';
        const MODULE_CLASSES = ['phy-255', 'cos-210', 'cos-212', 'wtw-211', 'wtw-218'];
        let editMode = false;

        // ── Firebase Setup ──
        const firebaseConfig = {
            apiKey: "AIzaSyArdb2Dm8ruW10w6bUYakIz75QuBhP1SD4",
            authDomain: "time-management-a4285.firebaseapp.com",
            projectId: "time-management-a4285",
            storageBucket: "time-management-a4285.firebasestorage.app",
            messagingSenderId: "453419428813",
            appId: "1:453419428813:web:c84c1e002db8beaa8a7284",
            measurementId: "G-K7HQWV2ZE5"
        };

        firebase.initializeApp(firebaseConfig);
        const auth = firebase.auth();
        const db = firebase.firestore();

        db.enablePersistence({ synchronizeTabs: true }).catch(err => {
            console.warn('Firestore persistence failed:', err.code);
        });

        let currentUser = null;

        function signInWithGoogle() {
            const provider = new firebase.auth.GoogleAuthProvider();
            auth.signInWithPopup(provider).catch(err => {
                console.error('Sign-in error:', err);
                alert('Sign-in failed: ' + err.message);
            });
        }

        function signOutUser() {
            auth.signOut();
        }

        auth.onAuthStateChanged(user => {
            currentUser = user;
            // Optionally update UI for auth state
            applyEdits();
        });

        function docRefEdits() {
            return db.collection('users').doc(currentUser.uid)
                     .collection('meta').doc(EDIT_STORAGE_KEY);
        }

        async function loadEdits() {
            if (currentUser) {
                try {
                    const snap = await docRefEdits().get();
                    if (snap.exists) {
                        localStorage.setItem(EDIT_STORAGE_KEY, JSON.stringify(snap.data().edits || {}));
                        return snap.data().edits || {};
                    }
                } catch (e) { console.warn('Firestore read failed:', e); }
            }
            try { return JSON.parse(localStorage.getItem(EDIT_STORAGE_KEY)) || {}; }
            catch { return {}; }
        }

        async function saveEdits(edits) {
            localStorage.setItem(EDIT_STORAGE_KEY, JSON.stringify(edits));
            if (currentUser) {
                try {
                    await docRefEdits().set({ edits }, { merge: true });
                } catch (e) { console.error('Firestore write error:', e); }
            }
        }

        function renderEventDisplay(eventEl, timeText, activityText, isOverride) {
            const timeEl = eventEl.querySelector('.time');
            const activityEl = eventEl.querySelector('.activity');
            timeEl.textContent = timeText;

            const defaultActivity = eventEl.dataset.defaultActivity || '';
            const defaultHref = eventEl.dataset.defaultHref || '';

            activityEl.innerHTML = '';
            if (!isOverride && defaultHref && activityText === defaultActivity) {
                const link = document.createElement('a');
                link.href = defaultHref;
                link.textContent = activityText;
                activityEl.appendChild(link);
            } else {
                activityEl.textContent = activityText;
            }
        }

        function detectModuleClass(text) {
            const val = text.toLowerCase();
            if (/\bphy\s*-?\s*255\b/.test(val)) return 'phy-255';
            if (/\bcos\s*-?\s*210\b/.test(val)) return 'cos-210';
            if (/\bcos\s*-?\s*212\b/.test(val)) return 'cos-212';
            if (/\bwtw\s*-?\s*211\b/.test(val)) return 'wtw-211';
            if (/\bwtw\s*-?\s*218\b/.test(val)) return 'wtw-218';
            return null;
        }

        function updateModuleClass(eventEl, activityText, isOverride) {
            MODULE_CLASSES.forEach(cls => eventEl.classList.remove(cls));
            const defaultModule = eventEl.dataset.defaultModule || '';
            const moduleClass = isOverride ? detectModuleClass(activityText) : defaultModule;
            if (moduleClass) eventEl.classList.add(moduleClass);

            if (activityText.trim()) {
                eventEl.classList.remove('empty');
            } else {
                eventEl.classList.add('empty');
            }
        }

        function persistEdit(eventEl, timeValue, activityValue) {
            const edits = loadEdits();
            const id = eventEl.dataset.eventId;
            const defaultTime = eventEl.dataset.defaultTime || '';
            const defaultActivity = eventEl.dataset.defaultActivity || '';

            if (timeValue === defaultTime && activityValue === defaultActivity) {
                delete edits[id];
            } else {
                edits[id] = { time: timeValue, activity: activityValue };
            }

            saveEdits(edits);
        }

        function applyEdits() {
            const edits = loadEdits();
            document.querySelectorAll('.event').forEach((eventEl, index) => {
                if (!eventEl.dataset.eventId) {
                    eventEl.dataset.eventId = String(index);
                }

                const timeEl = eventEl.querySelector('.time');
                const activityEl = eventEl.querySelector('.activity');
                const timeInput = eventEl.querySelector('.edit-time');
                const activityInput = eventEl.querySelector('.edit-activity');
                const hasInputs = Boolean(timeInput || activityInput);

                if (!eventEl.dataset.defaultTime && !hasInputs) {
                    eventEl.dataset.defaultTime = timeEl.textContent.trim();
                }

                if (!eventEl.dataset.defaultActivity && !hasInputs) {
                    const link = activityEl ? activityEl.querySelector('a') : null;
                    eventEl.dataset.defaultActivity = link ? link.textContent.trim() : activityEl.textContent.trim();
                    eventEl.dataset.defaultHref = link ? link.getAttribute('href') : '';
                }

                if (!eventEl.dataset.defaultModule && !hasInputs) {
                    const cls = MODULE_CLASSES.find(c => eventEl.classList.contains(c));
                    if (cls) eventEl.dataset.defaultModule = cls;
                }

                const defaultTime = eventEl.dataset.defaultTime || (timeInput ? timeInput.value.trim() : timeEl.textContent.trim());
                const defaultActivity = eventEl.dataset.defaultActivity || (activityInput ? activityInput.value.trim() : activityEl.textContent.trim());
                const defaultHref = eventEl.dataset.defaultHref || '';

                const override = edits[eventEl.dataset.eventId];
                const timeText = override && override.time ? override.time : defaultTime;
                const activityText = override && override.activity !== undefined ? override.activity : defaultActivity;
                eventEl.dataset.defaultHref = defaultHref;
                renderEventDisplay(eventEl, timeText, activityText, Boolean(override));
                updateModuleClass(eventEl, activityText, Boolean(override));
            });
        }

        function enterEditMode() {
            editMode = true;
            document.body.classList.add('edit-mode');
            const toggle = document.getElementById('editToggle');
            toggle.textContent = 'Done';
            toggle.setAttribute('aria-pressed', 'true');

            document.querySelectorAll('.event').forEach(eventEl => {
                const timeEl = eventEl.querySelector('.time');
                const activityEl = eventEl.querySelector('.activity');
                const timeValue = timeEl.textContent.trim();
                const activityValue = activityEl.textContent.trim();

                timeEl.innerHTML = '<input class="edit-time" type="text" value="' + timeValue.replace(/"/g, '&quot;') + '">';
                activityEl.innerHTML = '<input class="edit-activity" type="text" placeholder="Add activity" value="' + activityValue.replace(/"/g, '&quot;') + '">';

                const timeInput = timeEl.querySelector('.edit-time');
                const activityInput = activityEl.querySelector('.edit-activity');

                const autosave = () => {
                    const nextTime = timeInput.value.trim();
                    const nextActivity = activityInput.value.trim();
                    persistEdit(eventEl, nextTime, nextActivity);
                    updateModuleClass(eventEl, nextActivity, true);
                };

                timeInput.addEventListener('input', autosave);
                activityInput.addEventListener('input', autosave);
            });
        }

        function exitEditMode() {
            const edits = {};
            document.querySelectorAll('.event').forEach(eventEl => {
                const id = eventEl.dataset.eventId;
                const timeInput = eventEl.querySelector('.edit-time');
                const activityInput = eventEl.querySelector('.edit-activity');
                if (!timeInput || !activityInput) return;

                const timeValue = timeInput.value.trim();
                const activityValue = activityInput.value.trim();
                const defaultTime = eventEl.dataset.defaultTime || '';
                const defaultActivity = eventEl.dataset.defaultActivity || '';

                if (timeValue !== defaultTime || activityValue !== defaultActivity) {
                    edits[id] = { time: timeValue, activity: activityValue };
                }
            });

            saveEdits(edits);
            applyEdits();

            editMode = false;
            document.body.classList.remove('edit-mode');
            const toggle = document.getElementById('editToggle');
            toggle.textContent = 'Edit';
            toggle.setAttribute('aria-pressed', 'false');
            updateWhatsNext();
        }

        function setupEditToggle() {
            const toggle = document.getElementById('editToggle');
            if (!toggle) return;
            toggle.addEventListener('click', () => {
                if (editMode) {
                    exitEditMode();
                } else {
                    enterEditMode();
                }
            });
        }

        applyEdits();
        setupEditToggle();
        highlightToday();
        updateWhatsNext();
        if (window.innerWidth <= 768) {
            document.querySelectorAll('.event.empty').forEach(e => e.classList.add('hidden-empty'));
        }
        let whatsNextInterval = null;
        function startWhatsNextInterval() {
            if (whatsNextInterval) clearInterval(whatsNextInterval);
            whatsNextInterval = setInterval(updateWhatsNext, 60000);
        }

        document.addEventListener('visibilitychange', () => {
            if (document.visibilityState === 'visible') {
                updateWhatsNext();
                startWhatsNextInterval();
            } else {
                if (whatsNextInterval) clearInterval(whatsNextInterval);
            }
        });

        startWhatsNextInterval();
    </script>
</body>
</html>